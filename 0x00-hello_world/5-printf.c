#include <stdio.h>
/**
 * main - Write a C program that prints exactly with proper grammar,
 * but the outcome is a piece of art, follwed by a new line
 * Return: 0, if everything executes properly
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,");
	return (0);
}
