#include <stdlib.h>
#include <stdio.h>
/**
 * main - function that prints the file name
 * that gcc compiled
 * Return: 0, if everything executes properly
 */
int main(void)
{
	printf("%s\n", __FILE__);
	return (0);
}
